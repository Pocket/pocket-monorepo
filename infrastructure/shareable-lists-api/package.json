{
  "name": "shareable-lists-api-cdk",
  "version": "1.0.0",
  "main": "dist/main.js",
  "types": "dist/main.ts",
  "license": "MPL-2.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && tsc",
    "watch": "tsc -w",
    "lint": "eslint --fix-dry-run \"src/**/*.ts\"",
    "format": "eslint --fix \"src/**/*.ts\"",
    "synth": "cdktf synth",
    "compile": "tsc --pretty"
  },
  "dependencies": {
    "@pocket-tools/terraform-modules": "workspace:*"
  },
  "devDependencies": {
    "tsconfig": "workspace:*",
    "@pocket-tools/eslint-config": "workspace:*"
  }
}