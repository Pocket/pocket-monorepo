{
  "private": true,
  "scripts": {
    "build": "dotenv -- turbo run build",
    "dev": "dotenv -- turbo run dev",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "clean": "rm -rf node_modules packages/**/.turbo packages/**/dist packages/**/node_modules servers/**/logs servers/**/.turbo servers/**/dist servers/**/node_modules servers/**/.aws/.turbo servers/**/.aws/dist servers/**/.aws/node_modules"
  },
  "devDependencies": {
    "dotenv-cli": "latest",
    "eslint": "^8.55.0",
    "prettier": "^3.1.0",
    "tsconfig": "workspace:*",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.6.10",
  "name": "pocket-monorepo",
  "engines": {
    "node": "^20"
  }
}
