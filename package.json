{
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "dotenv -- turbo run build",
    "dev": "dotenv -- turbo run dev",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "test": "dotenv -- turbo run test",
    "test-integrations": "dotenv -- turbo run test-integrations",
    "synth:infra:prod": "dotenv -- turbo run synth:infra:prod",
    "synth:infra:dev": "dotenv -- turbo run synth:infra:dev",
    "clean": "rm -rf .turbo node_modules packages/**/.turbo packages/**/dist packages/**/node_modules servers/**/logs servers/**/.turbo servers/**/dist servers/**/node_modules infrastructure/**/dist infrastructure/**/node_modules infrastructure/**/.turbo"
  },
  "dependencies": {
    "dotenv-cli": "latest"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.12.0",
  "name": "pocket-monorepo",
  "engines": {
    "node": "^20"
  }
}
