query searchSavedItemsSimple(
  $term: String!
  $pagination: OffsetPaginationInput
  $filter: SearchFilterInput
  $sort: SearchSortInput
  $withAnnotations: Boolean!
) {
  user {
    searchSavedItemsByOffset(
      term: $term
      pagination: $pagination
      filter: $filter
      sort: $sort
    ) {
      entries {
        savedItem {
          ...SavedItemSimple
          ...HighlightFields @include(if: $withAnnotations)
        }
        ...SearchResultHighlights
      }
      offset
      limit
      totalCount
    }
  }
}

# query searchSavedItemsSimpleAnnotations(
#   $term: String!
#   $pagination: OffsetPaginationInput
#   $filter: SearchFilterInput
#   $sort: SearchSortInput
# ) {
#   user {
#     searchSavedItemsByOffset(
#       term: $term
#       pagination: $pagination
#       filter: $filter
#       sort: $sort
#     ) {
#       entries {
#         savedItem {
#           ...SavedItemSimple
#           ...HighlightFields
#         }
#         ...SearchResultHighlights
#       }
#       offset
#       limit
#       totalCount
#     }
#   }
# }
